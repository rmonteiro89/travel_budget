<%= render 'trips/add_trip' %>
<br>
<div class="row">
  <% if @trips.present? %>
    <%= render(@trips) %>
  <% else %>
    <div class="col-md-12">
      <p class='alert alert-info'><%= "Click on the button with the plus icon above to add your first trip." %></p>
    </div>
  <% end %>
</div>
